[target.xtensa-esp32-none-elf]
runner = "espflash flash --monitor --chip esp32 --flash-size 8mb --partition-table partitions.csv"

[env]
ESP_LOG="info"

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
]

target = "xtensa-esp32-none-elf"

[unstable]
build-std = ["alloc", "core"]
